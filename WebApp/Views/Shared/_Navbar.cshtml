<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
        <li><a href="@Url.Action("Index")" class="custom-link">BanCobradotas</a></li>
        </li>
        @if (Context.User.Identity?.IsAuthenticated ?? false)
        {
          <li class="nav-item">
          <li><a href="@Url.Action("LogOut", "LogIn")" class="custom-link">Salir de la cuenta</a></li>
          </li>
        }
        else
        {
          <li class="nav-item">
            <a href="@Url.Action("IniciarSesion", "LogIn")" class="custom-link">Iniciar Sesion</a>
          </li>
        }
      </ul>
    </div>
  </div>
</nav>

